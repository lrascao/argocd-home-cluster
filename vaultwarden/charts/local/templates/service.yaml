apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: {{ .Values.namespace }}
  labels:
    app: vaultwarden
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: vaultwarden
