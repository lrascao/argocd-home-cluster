apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-plex-data
  namespace: plex
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
