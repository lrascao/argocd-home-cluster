apiVersion: v1
kind: Service
metadata:
  namespace: nodered
  labels:
    app: nodered
  name: nodered
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 1880
  selector:
    app: nodered
